# Makefile.am --
########################################################################

INPUT = \
   fail.input \
   flat.input moderate.input steep.input low.input \
   power.input resonance.input \
   multi.input delta.input \
   smooth.input oversmooth.input \
   scale.input

CHANNELS = $(INPUT:.input=.channels)

EXTRA_DIST = $(INPUT) $(CHANNELS)
